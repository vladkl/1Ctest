
&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	
	Если Не Контекст.МониторингВключен() Тогда
		ФормаМонитора = ФормаМонитора(Истина);
		
		ПараметрыКоманды = Новый Структура;
		ПараметрыКоманды.Вставить("Владелец", ФормаМонитора);
		ФормаМонитора.ВыполнитьКоманду(ПредопределенноеЗначение("Справочник.Команды.ВыполнитьСценарий"), ПараметрыКоманды);
	КонецЕсли;
	
КонецПроцедуры
