
&НаСервере
Процедура СписокПриАктивизацииСтрокиНаСервере()
	
	ТекСтр = ЭтотОбъект.Элементы.Список.ТекущаяСтрока;
	НаборЗаписей = РегистрыСведений.ДанныеТЖ.СоздатьНаборЗаписей();
	НаборЗаписей.Отбор.Регистратор.Установить(ТекСтр.Регистратор);
	НаборЗаписей.Прочитать();
	
КонецПроцедуры

&НаКлиенте
Процедура СписокПриАктивизацииСтроки(Элемент)
	СписокПриАктивизацииСтрокиНаСервере();
КонецПроцедуры
